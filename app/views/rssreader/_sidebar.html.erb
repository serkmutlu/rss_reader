<h6><small>BLOG</small></h6>
<% @rss.where("category = ?", "Blog").each do |address| %>
  <h4><small><%= link_to address.friendly_name, address %></small></h4>
<% end %>

<h6 style="margin-top: 30px;"><small>FUN</small></h6>
<% @rss.where("category = ?", "Fun").each do |address| %>
  <h4><small><%= link_to address.friendly_name, address %></small></h4>
<% end %>

<h6 style="margin-top: 30px;"><small>NEWS</small></h6>
<% @rss.where("category = ?", "News").each do |address| %>
  <h4><small><%= link_to address.friendly_name, address %></small></h4>
<% end %>

<% if user_signed_in? %>
<p style="margin-top:32px;"><%= link_to "+ Add new", new_rssreader_path, class: "button tiny" %></p>
<% end %>